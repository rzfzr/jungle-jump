using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IPHAchievementsButton : MonoBehaviour {
    // We find IPHAchievements every time it starts, could have it also persistent 
    // public void SetCanvas () {
    //     GameObject canvas;
    //     canvas = GameObject.Find ("CanvasAchievements");
    //     canvas.SetActive (true);
    // }
}